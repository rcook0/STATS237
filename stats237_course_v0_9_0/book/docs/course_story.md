# Course Story

This file is generated from `discourse/course_story.md` during the book build.

Run:

```bash
python scripts/build_book.py
```

Then rebuild the site:

```bash
mkdocs -f book/mkdocs.yml build
```
